{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "{{typeField}}",
  "scripts": {
    "prepare": "simple-git-hooks"{{scripts}}
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "**/*.{js,ts,svelte,json,css,html,md}": "prettier --write --ignore-unknown",
    "**/*.{js,ts,svelte}": "eslint --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    {{devDependencies}}
  }{{overrides}}
}
